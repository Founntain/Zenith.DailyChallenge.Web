interface Split {
  averageTime: string;
  bestTime: string;
  bestTimeAchievedDate: string;
}

export interface ZenithSplits {
  hotel: Split
  casino: Split;
  arena: Split;
  museum: Split;
  offices: Split;
  laboratory: Split;
  core: Split;
  corruption: Split;
  potg: Split;
}
